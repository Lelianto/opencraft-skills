# Product decisions

Store one immutable-history JSON record per material decision. Use `DEC-<DOMAIN>-NNN` identifiers, preserve alternatives and consequences, and update the status instead of deleting rejected or superseded choices.
